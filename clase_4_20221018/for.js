// for (deficion; condicion; aumento) { accion }


for (let step = 1; step < 6; step = step + 1) {
    // Se ejecuta 5 veces, con valores del paso 0 al 4.
    console.log('Camina un paso hacia el este' + step);
}